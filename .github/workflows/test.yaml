name: Tests

on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'src-docs/**'

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
