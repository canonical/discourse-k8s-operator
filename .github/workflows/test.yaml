name: Tests

on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'src-docs/**'
      - 'README.md'
      - 'LICENSE'
      - 'CODEOWNERS'

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
