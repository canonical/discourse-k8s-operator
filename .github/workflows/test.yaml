name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@ISD-4247-remove-inclusive-check
    secrets: inherit
    with:
      self-hosted-runner: true
      self-hosted-runner-label: "edge"
