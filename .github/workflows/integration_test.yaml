name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@feature/allow-remote-debug
    secrets: inherit
    with:
      pre-run-script: localstack-installation.sh