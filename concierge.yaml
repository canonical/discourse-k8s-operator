# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

juju:
  channel: 3.6/stable
  model-defaults:
    test-mode: "true"
    automatically-retry-hooks: "false"

providers:
  microk8s:
    enable: true
    channel: 1.34-strict
    bootstrap: true
    addons:
      - hostpath-storage
      - dns
      - ingress
host:
  snaps:
    rockcraft:
